import { LearningShell } from "@/components/demo/learning/learning-shell";

export default function LearningLayout({ children }: { children: React.ReactNode }) {
  return <LearningShell>{children}</LearningShell>;
}
